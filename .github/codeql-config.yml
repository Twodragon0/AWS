name: "CodeQL Configuration"

# CodeQL 분석 쿼리 설정
queries:
  # 기본 보안 쿼리 사용
  - uses: security-and-quality

# 참고: 파일 제외는 워크플로우의 paths-ignore에서 처리됩니다.
# CodeQL은 기본적으로 .gitignore를 존중하므로
# node_modules는 자동으로 제외됩니다.
