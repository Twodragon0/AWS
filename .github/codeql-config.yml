name: "CodeQL Configuration"

# CodeQL 분석 쿼리 설정
queries:
  # 기본 보안 쿼리 사용
  - uses: security-and-quality

# CodeQL은 기본적으로 .gitignore를 존중하므로
# node_modules는 자동으로 제외됩니다.
# 추가 제외가 필요한 경우 워크플로우에서 paths-ignore 사용
